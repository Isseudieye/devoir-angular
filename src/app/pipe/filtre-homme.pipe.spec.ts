import { FiltreHommePipe } from './filtre-homme.pipe';

describe('FiltreHommePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreHommePipe();
    expect(pipe).toBeTruthy();
  });
});
